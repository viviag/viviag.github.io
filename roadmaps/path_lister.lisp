(load "criterias_for_sorting.lisp")

;Returns all paths from first arg to second via map from third.
;Sorting by length while construct.
;append to list only correct. use high order recursion
(defun generate_paths (List)
)
